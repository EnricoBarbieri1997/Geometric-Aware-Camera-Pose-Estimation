\section{Related work}

Several methods have addressed the estimation of camera intrinsics and pose from geometric primitives such as lines and conics. \emph{Line-based approaches} include early works as~\cite{hartley93lines}, which jointly estimate $\KK$ and 3D structure from multi-view line correspondences.  Interestingly, Guo et al.~\cite{guo23vanishing}  simplify the problem by assuming a known camera center and using two vanishing points to estimate the rotation $\RR$ and focal length $f$. While the input of their method is similar to our, as the two  3D vanishing points can be identified with the two cylinder axis direction, their approach does not recover the translation, $\mathbf{t}$ nor the full set of intrinsic parameters. \emph{Conic-based methods} exploit the relation between conic sections -- typically circles -- on  planar calibration targets, and their projections on image planes  to improve the accuracy and robustness of corner-based calibration procedure (\emph{e.g.},\cite{chsong24}).

Our focus is on the less explored  class of \emph{quadric-based} approaches that leverage the multi-view constraint provided by 3D quadrics  as calibration and localization cues.
We consider methods based on degenerate quadrics such as cylinders, whose surfaces project to line silhouettes under perspective projection. Gummeson et al.~\cite{gummeson22pose} propose minimal solvers for pose estimation using line silhouettes, but assume known intrinsics. Interestingly,  under some circumstances, they show that they are also able to recover the focal $f$. Their more recent work~\cite{gummeson24relative} focuses on relative pose from silhouettes across two views, again assuming fixed intrinsics that must be known in advance. Our approach is directly inspired by these methods but goes further by addressing the full calibration problem. Moreover, rather than rely on Grobner basis, we exploits the advantages of homotopy continuation to geometrically inform the solution process. 
A further relevant contribution is the work by Larsson et al.~\cite{larsson20parallelcylinders}, which investigates the multi-view geometry of mutually parallel cylinders. Their formulation allows a structure-from-motion pipeline based on silhouette lines, under the strong assumption that all cylinders share a common axis direction $\mathbf{w}_j=\mathbf{w}_k$ for all $j,k=1,\ldots, M$. The emphasis  is on recovering $\mathsf{R}_i,\mathbf{t}_i$ and the  parameters of the cylinders $\mathcal{C}_j$ up to scale, assuming known intrinsics. In contrast, our method handles arbitrary cylinder orientations, assume the cylinders to be known and jointly estimates both intrinsic and extrinsic parameters, enabling complete camera resection.
Cylinder sections are exploited by Ding et al. in~\cite{CalibrationFreePlacedRightCylinder}. This method leverages the known geometry of the target—specifically, the circular end-faces of the cylinder—and formulates constraints based on their elliptical projections. Table~\ref{tab:soa} summarizes the most relevant methods in terms of their output. Only our approach estimates both the full camera calibration $\mathsf{K}$ and the camera poses $\mathsf{R}_i,\mathbf{t}_i.$

\begin{table}[tb]
\begin{center}
\resizebox{0.6\textwidth}{!}{%
\begin{tabular}{@{}lllll@{}}
\toprule
                & Guo\cite{guo23vanishing}      & Ding \cite{CalibrationFreePlacedRightCylinder}        & Gummerson   \cite{gummeson22pose,gummeson24relative}           & Ours                    \\ \midrule
Calibration     & $f$ \mehmark & $\mathsf{K}$ \cmark&  $f$  \mehmark             & $\mathsf{K} $     \cmark       \\
Pose estimation &  $\mathsf{R}$ \mehmark & \xmark     & $\mathsf{R},\mathbf{t}$ \cmark & $\mathsf{R},\mathbf{t}$ \cmark\\
\bottomrule
\end{tabular}%
}
\end{center}
\caption{ Comparison of camera estimation methods and their outputs for calibration and pose estimation. (\xmark: not addressed, \mehmark: partial output, \cmark: fully solved). Our method uniquely solves both tasks and provides all parameters.}
\label{tab:soa}
\end{table}
